# base\services\gateway\deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: zerodayz7/obywatel-plus-gateway:latest
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: gateway-config
          env:
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: jwt-access-secret
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: jwt-refresh-secret
            - name: INTERNAL_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: internal-hmac-secret
            - name: INTERNAL_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: internal-encryption-key
            - name: INTERNAL_HASH_SALT
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: internal-hash-salt
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: redis-password
          resources:
            limits:
              cpu: "200m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 10
