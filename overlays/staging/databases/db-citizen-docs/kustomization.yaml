apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../base/infra/databases/db-citizen-docs

patches:
  - path: patch-storage.yaml
    target:
      kind: PersistentVolumeClaim
      name: db-citizen-docs-pvc
